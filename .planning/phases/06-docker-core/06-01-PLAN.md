---
phase: 06-docker-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/50-docker/50-docker-engine.sh
autonomous: true
requirements:
  - DOCK-01
  - DOCK-02
must_haves:
  truths:
    - Docker Engine is installed from official Docker repository
    - Docker CLI is available at /usr/bin/docker
    - Idempotency: skipping if docker already installed
    - Bash syntax is valid
  artifacts:
    - path: modules/50-docker/50-docker-engine.sh
      provides: Docker Engine and CLI installation
      min_lines: 50
  key_links: []
---

## Objective

Install Docker Engine and Docker CLI from official Docker apt repository.

**Purpose:** Provide the core Docker platform for containerization on the server.
**Output:** 50-docker-engine.sh module installed and executable.

## Context

### Requirements
- DOCK-01: Docker Engine installed and daemon ready
- DOCK-02: Docker CLI available for all admin users

### From CONTEXT.md
- Install from Docker's official apt repository (not Ubuntu universe)
- Packages: docker-ce, docker-ce-cli, containerd.io
- Add Docker's GPG key and apt repository
- Module: 50-docker-engine.sh in modules/50-docker/

### Codebase Patterns
From modules/40-dev/10-node.sh:
- Use source guard: `source "$(dirname "$0")/../../lib/common.sh"`
- Set `set -euo pipefail`
- Idempotency: Check with `command -v docker` before installation
- Use `DEBIAN_FRONTEND=noninteractive` for apt
- Use `log_info` from common.sh for status messages
- Use `die` for error handling

From lib/common.sh:
- Colors defined, log_info/log_warn/log_error functions
- check_root() for root validation

### Docker Installation Steps
1. Check if Docker already installed (`command -v docker`)
2. Install prerequisites: ca-certificates, curl, gnupg
3. Add Docker's official GPG key
4. Add Docker repository to apt sources
5. Install docker-ce, docker-ce-cli, containerd.io
6. Verify installation with `docker --version`

## Tasks

<task type="auto">
  <name>Task 1: Create Docker directory structure</name>
  <files>modules/50-docker/50-docker-engine.sh</files>
  <action>
    Create modules/50-docker/ directory if not exists.
    Create 50-docker-engine.sh following codebase patterns:
    - #!/bin/bash header with description
- set -euo pipefail
    - source lib/common.sh with relative path
    - install_docker_engine() function
    - Check if docker command exists first (idempotency)
    - Install prerequisites: ca-certificates, curl, gnupg
    - Add Docker GPG key via curl | gpg --dearmor
    - Add apt source for docker
    - Install docker-ce, docker-ce-cli, containerd.io
    - Verify installation
    - main() wrapper
    
    Follow same structure as 40-dev/10-node.sh
  </action>
  <verify>
    File exists with correct header
    <automated>head -20 /home/bryan/Projects/Brylly/brybuntu-server/modules/50-docker/50-docker-engine.sh | head -1 | grep "#!/bin/bash"</automated>
  </verify>
  <done>50-docker-engine.sh exists with proper structure and shellcheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Make module executable and verify</name>
  <files>modules/50-docker/50-docker-engine.sh</files>
  <action>
    1. chmod +x modules/50-docker/50-docker-engine.sh
    2. bash -n to syntax check
    3. File references Docker GPG key setup
  </action>
  <verify>
    Module is executable and syntax valid
    <automated>bash -n /home/bryan/Projects/Brylly/brybuntu-server/modules/50-docker/50-docker-engine.sh && ls -l /home/bryan/Projects/Brylly/brybuntu-server/modules/50-docker/50-docker-engine.sh | grep -E "^-rwx"</automated>
  </verify>
  <done>50-docker-engine.sh executable and passes bash -n syntax check</done>
</task>

## Verification
- [ ] modules/50-docker/ directory exists
- [ ] 50-docker-engine.sh is executable
- [ ] Script has proper header and source guard
- [ ] Script checks for existing docker before install (idempotency)
- [ ] Uses lib/common.sh for logging
- [ ] Follows DOCKER-01 requirement for Docker Engine
- [ ] Follows DOCK-02 for CLI availability

## Success Criteria
- 50-docker-engine.sh module exists and is executable
- Module follows all codebase patterns
- DOCK-01 and DOCK-02 requirements addressed

## Output
After execution, create:
- `.planning/phases/06-docker-core/06-01-SUMMARY.md`
