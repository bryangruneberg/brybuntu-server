---
phase: 06-docker-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/50-docker/51-docker-compose.sh
autonomous: true
requirements:
  - DOCK-03
must_haves:
  truths:
    - Docker Compose v2 plugin is installed
    - 'docker compose version' command works
    - Idempotency: skip if already installed
    - Bash syntax is valid
  artifacts:
    - path: modules/50-docker/51-docker-compose.sh
      provides: Docker Compose plugin installation
      min_lines: 40
  key_links: []
---

## Objective

Install Docker Compose v2 as Docker CLI plugin.

**Purpose:** Provide modern Docker Compose support for multi-container orchestration.
**Output:** 51-docker-compose.sh module installed and executable.

## Context

### Requirements
- DOCK-03: Docker Compose plugin installed

### From CONTEXT.md
- Install as Docker Compose v2 CLI plugin (modern approach)
- Command: docker compose (space, not hyphen)
- Installed via apt from Docker repository (docker-compose-plugin package)

### Codebase Patterns
From modules/40-dev/10-node.sh:
- Same header, set -euo pipefail
- Use lib/common.sh
- Idempotency check with command -v or dpkg -l
- Install with apt-get

Docker Compose specific:
- Package name: docker-compose-plugin
- Check: docker compose version (with space)
- No separate command line, runs via docker compose

## Tasks

<task type="auto">
  <name>Task 1: Create Docker Compose module</name>
  <files>modules/50-docker/51-docker-compose.sh</files>
  <action>
    Create 51-docker-compose.sh in modules/50-docker/:
    1. #!/bin/bash header with description
    2. set -euo pipefail
    3. Source lib/common.sh
    4. install_docker_compose() function:
       - Check if docker compose is available (docker compose version 2>/dev/null)
       - If installed, log and return 0 (idempotency)
       - Install docker-compose-plugin via apt
       - Verify with docker compose version
    5. main() calling install_docker_compose
    6. Use log_info for progress
    7. Use die for errors
    
    Script is standalone, runs independently of 50-docker-engine.sh
    (both use apt, can run parallel if engine already installed)
  </action>
  <verify>
    <automated>bash -n /home/bryan/Projects/Brylly/brybuntu-server/modules/50-docker/51-docker-compose.sh</automated>
  </verify>
  <done>51-docker-compose.sh exists and is valid bash</done>
</task>

<task type="auto">
  <name>Task 2: Verify and commit module</name>
  <files>modules/50-docker/51-docker-compose.sh</files>
  <action>
    1. chmod +x on the module
    2. Run bash -n to validate
    3. Module uses lib/common.sh
    4. Calls docker compose version for verification
  </action>
  <verify>
    <automated>test -x /home/bryan/Projects/Brylly/brybuntu-server/modules/50-docker/51-docker-compose.sh && echo "PASS: file is executable" || echo "FAIL: file not executable"</automated>
  </verify>
  <done>51-docker-compose.sh executable and ready</done>
</task>

## Verification
- [ ] 51-docker-compose.sh exists
- [ ] Uses dpkg -l docker-compose-plugin for idempotency
- [ ] Installs docker-compose-plugin via apt
- [ ] Verifies with docker compose version

## Success Criteria
- 51-docker-compose.sh module ready
- Module addresses DOCK-03

## Output
After execution, create:
- `.planning/phases/06-docker-core/06-02-SUMMARY.md`
