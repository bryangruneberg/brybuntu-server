---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/common.sh
  - install.sh
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - "Common library provides logging, error handling, and idempotency utilities"
    - "Main orchestrator script exists and can be executed"
    - "Orchestrator discovers numbered modules and executes them in order"
  artifacts:
    - path: "lib/common.sh"
      provides: "Logging, error handling, idempotency utilities with source guard"
      exports: ["log_info", "log_warn", "log_error", "die", "check_root", "validate_ubuntu"]
    - path: "install.sh"
      provides: "Main orchestrator that discovers and executes 10-*.sh, 20-*.sh modules"
      min_lines: 50
  key_links:
    - from: "install.sh"
      to: "lib/common.sh"
      via: "source ${SCRIPT_DIR}/lib/common.sh"
      pattern: "source.*lib/common"
    - from: "install.sh"
      to: "modules/"
      via: "find + sort discovery of numbered scripts"
      pattern: "find.*modules.*sort"
---

<objective>
Create the foundational execution framework: a shared library with logging/error handling utilities and a main orchestrator script that discovers and executes numbered modules in order.

Purpose: Establishes the core infrastructure pattern used by all subsequent phases - modular, idempotent, well-logged bash automation.
Output: lib/common.sh and install.sh ready for module execution.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

## Research Patterns to Follow

From RESEARCH.md:
- Use `set -euo pipefail` in all scripts
- Shared library with source guard: `if [[ -n "${BRYBUNTU_COMMON:-}" ]]; then return 0; fi`
- Module discovery: `find "$modules_dir" -maxdepth 2 -name "[0-9][0-9]-*.sh" -type f | sort -V`
- Explicit error checking beyond `set -e`: `command || die "message"`
- Colors: Check `[[ -t 1 ]]` for TTY before using ANSI codes

## SSH Key from PROJECT.md
- Ed25519 key: `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKt9pdXZ/aI31oyRrCc7ER8pfTOcS3r04xVnEOmEjhss bryan@bryarchy`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared library (lib/common.sh)</name>
  <files>lib/common.sh</files>
  <behavior>
    - Library can be sourced without errors
    - Double-sourcing returns early (no redefinition errors)
    - log_info outputs "[INFO] message" in green (if TTY)
    - log_warn outputs "[WARN] message" in yellow to stderr (if TTY)
    - log_error outputs "[ERROR] message" in red to stderr (if TTY)
    - die logs error and exits with code 1
    - check_root verifies EUID is 0, dies if not
    - validate_ubuntu checks /etc/os-release for ID=ubuntu
  </behavior>
  <action>
    Create lib/common.sh following these specifications:
    1. Source guard: `if [[ -n "${BRYBUNTU_COMMON:-}" ]]; then return 0; fi` then `readonly BRYBUNTU_COMMON=1`
    2. Color definitions: Check `[[ -t 1 ]]` to detect TTY, define RED/GREEN/YELLOW/NC or empty strings
    3. Logging functions: log_info, log_warn, log_error with color and appropriate output stream
    4. die(): Logs to stderr and exits 1
    5. check_root(): Verifies $EUID -eq 0, dies with "Must run as root" if not
    6. validate_ubuntu(): Sources /etc/os-release, checks ID=ubuntu, warns if VERSION_ID != 24.04
    
    Include `set -euo pipefail` behavior via strict mode recommendations in comments.
    All functions must be shellcheck-compliant.
  </action>
  <verify>
    <automated>bash -n lib/common.sh && shellcheck lib/common.sh</automated>
  </verify>
  <done>lib/common.sh exists, passes shellcheck, provides all required functions with source guard</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create main orchestrator (install.sh)</name>
  <files>install.sh</files>
  <behavior>
    - Script discovers modules in modules/ directory
    - Only executes files matching [0-9][0-9]-*.sh pattern
    - Executes in lexicographic (version) sort order
    - Stops on first failure (fail-fast)
    - Outputs count of executed modules
    - Exits with code 0 on success, non-zero on failure
  </behavior>
  <action>
    Create install.sh following RESEARCH.md patterns:
    1. Shebang and strict mode: `#!/bin/bash` + `set -euo pipefail`
    2. SCRIPT_DIR: `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
    3. Source common library: `source "${SCRIPT_DIR}/lib/common.sh"`
    4. discover_modules(): Find [0-9][0-9]-*.sh files under modules/, sort -V
    5. main():
       - Call check_root and validate_ubuntu
       - Log start message
       - Discover modules, die if none found
       - Execute each module with `bash "$script" || die "Module failed: $script"`
       - Count executed modules, log completion
    
    Ensure robust error handling and clear logging.
  </action>
  <verify>
    <automated>bash -n install.sh && shellcheck install.sh</automated>
  </verify>
  <done>install.sh exists, passes shellcheck, sources common.sh, implements module discovery and execution</done>
</task>

</tasks>

<verification>
1. Both scripts pass ShellCheck: `shellcheck lib/common.sh install.sh`
2. Syntax validation: `bash -n lib/common.sh && bash -n install.sh`
3. Library loads without error: `bash -c 'source lib/common.sh && log_info "test ok"'`
4. Scripts have correct permissions: executable bit set on install.sh
</verification>

<success_criteria>
- lib/common.sh exists with source guard and all required functions
- install.sh exists and can source common.sh without errors
- Module discovery logic finds files matching [0-9][0-9]-*.sh pattern
- Execution stops on first failure (fail-fast behavior)
- All code passes ShellCheck static analysis
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md`
</output>
