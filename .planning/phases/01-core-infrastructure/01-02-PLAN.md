---
phase: 01-core-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - modules/10-system/10-update.sh
  - modules/10-system/20-packages.sh
autonomous: false
requirements:
  - CORE-04
  - SYS-01
  - SYS-02
  - SYS-03
must_haves:
  truths:
    - "System update module runs apt update automatically"
    - "Package installation module installs kitty-terminfo idempotently"
    - "Running install.sh twice produces no errors (idempotent)"
    - "All operations have explicit error checking beyond set -e"
  artifacts:
    - path: "modules/10-system/10-update.sh"
      provides: "Idempotent apt update with DEBIAN_FRONTEND=noninteractive"
      exports: ["update_system"]
    - path: "modules/10-system/20-packages.sh"
      provides: "Idempotent package installation for kitty-terminfo"
      exports: ["install_packages"]
  key_links:
    - from: "10-update.sh"
      to: "lib/common.sh"
      via: "source $(dirname $0)/../../lib/common.sh"
      pattern: "source.*common.sh"
    - from: "20-packages.sh"
      to: "lib/common.sh"
      via: "source $(dirname $0)/../../lib/common.sh"
      pattern: "source.*common.sh"
    - from: "install.sh"
      to: "10-update.sh"
      via: "module discovery finds 10-*.sh before 20-*.sh"
      pattern: "find.*sort.*-V"
---

<objective>
Create system setup modules that handle apt update and package installation with full idempotency and error handling. Verify idempotency through test execution.

Purpose: Complete the core infrastructure phase with working system update and package installation, demonstrating the modular, idempotent pattern for all future modules.
Output: modules/10-system/ with working update and package scripts, idempotency verified.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

## From Prior Plan (01-01)
- lib/common.sh provides: log_info, log_warn, log_error, die, check_root, validate_ubuntu
- install.sh discovers and executes modules matching [0-9][0-9]-*.sh in version sort order
- Scripts use: `source "$(dirname "$0")/../../lib/common.sh"` to load utilities

## Idempotency Pattern from RESEARCH.md
```bash
install_package() {
    local pkg="$1"
    if dpkg -l "$pkg" 2>/dev/null | grep -q "^ii"; then
        log_info "$pkg already installed, skipping"
        return 0
    fi
    apt-get install -y "$pkg" || die "Failed to install $pkg"
}
```

## Non-Interactive apt Pattern
```bash
export DEBIAN_FRONTEND=noninteractive
apt-get update -qq || die "apt update failed"
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create system update module (10-update.sh)</name>
  <files>modules/10-system/10-update.sh</files>
  <behavior>
    - Sources common.sh from relative path
    - Sets DEBIAN_FRONTEND=noninteractive
    - Runs apt-get update -qq
    - Dies with clear message if apt update fails
    - Logs progress messages
    - Is safe to run multiple times (idempotent by nature of apt update)
  </behavior>
  <action>
    Create modules/10-system/10-update.sh:
    1. Shebang: `#!/bin/bash` + `set -euo pipefail`
    2. Source common library: `source "$(dirname "$0")/../../lib/common.sh"`
    3. update_system():
       - Log "Updating package lists..."
       - `export DEBIAN_FRONTEND=noninteractive`
       - `apt-get update -qq || die "apt update failed"`
       - Log "System update complete"
    4. main():
       - Call update_system
    5. Execute main
    
    Use -qq for apt to be quiet but still show errors.
  </action>
  <verify>
    <automated>bash -n modules/10-system/10-update.sh && shellcheck modules/10-system/10-update.sh</automated>
  </verify>
  <done>10-update.sh exists, passes shellcheck, implements DEBIAN_FRONTEND and apt update with error handling</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create package installation module (20-packages.sh)</name>
  <files>modules/10-system/20-packages.sh</files>
  <behavior>
    - Sources common.sh from relative path
    - Checks if kitty-terminfo is already installed via dpkg -l
    - Skips installation if already present (logs skip message)
    - Installs with apt-get install -y -qq if not present
    - Dies with clear message if installation fails
    - Logs progress messages
  </behavior>
  <action>
    Create modules/10-system/20-packages.sh:
    1. Shebang: `#!/bin/bash` + `set -euo pipefail`
    2. Source common library: `source "$(dirname "$0")/../../lib/common.sh"`
    3. install_packages():
       - Set DEBIAN_FRONTEND=noninteractive
       - Check if kitty-terminfo installed: `dpkg -l kitty-terminfo 2>/dev/null | grep -q "^ii"`
       - If installed: log_info "kitty-terminfo already installed, skipping"
       - If not: log_info "Installing kitty-terminfo..." then `apt-get install -y -qq kitty-terminfo || die "Failed to install kitty-terminfo"`
       - Log "Package installation complete"
    4. main():
       - Call install_packages
    5. Execute main
    
    The dpkg check pattern makes this idempotent.
  </action>
  <verify>
    <automated>bash -n modules/10-system/20-packages.sh && shellcheck modules/10-system/20-packages.sh</automated>
  </verify>
  <done>20-packages.sh exists, passes shellcheck, implements idempotent kitty-terminfo installation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify idempotency and integration</name>
  <what-built>
    Complete Phase 1 core infrastructure:
    - lib/common.sh with logging utilities
    - install.sh orchestrator with module discovery
    - modules/10-system/10-update.sh (apt update)
    - modules/10-system/20-packages.sh (kitty-terminfo)
  </what-built>
  <how-to-verify>
    Test on an Ubuntu system (VM, container, or actual server):
    
    1. **First run test:**
       ```bash
       cd /path/to/brybuntu-server
       sudo ./install.sh
       ```
       Expected: Executes 10-update.sh then 20-packages.sh, shows success messages
    
    2. **Idempotency test (run again):**
       ```bash
       sudo ./install.sh
       ```
       Expected: Completes without errors, should show "already installed" for packages
    
    3. **Verify kitty-terminfo:**
       ```bash
       dpkg -l | grep kitty-terminfo
       ```
       Expected: Shows package as installed
    
    4. **Check execution order:**
       Confirm 10-update.sh ran before 20-packages.sh in output
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. All modules pass ShellCheck: `shellcheck modules/10-system/*.sh`
2. Module files have correct permissions: executable
3. Directory structure exists: modules/10-system/
4. Idempotency verified: Second run produces no errors
5. kitty-terminfo package installed: `dpkg -l kitty-terminfo` shows "^ii"
</verification>

<success_criteria>
- modules/10-system/10-update.sh exists and passes shellcheck
- modules/10-system/20-packages.sh exists and passes shellcheck
- install.sh discovers and executes both modules in correct order
- Running install.sh twice produces no errors (idempotent)
- kitty-terminfo package is installed after first run
- All scripts have explicit error checking (|| die pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-02-SUMMARY.md`
</output>
