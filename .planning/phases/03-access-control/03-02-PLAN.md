---
phase: 03-access-control
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - modules/30-sudo/20-amazeeio-sudo.sh
  - /etc/sudoers.d/amazeeio
autonomous: true
requirements:
  - SUDO-02
  - SUDO-03
  - SUDO-04
must_haves:
  truths:
    - "amazeeio user has passwordless sudo access"
    - "Sudoers syntax is validated before file installation"
    - "Sudoers file has correct permissions (440)"
  artifacts:
    - path: "modules/30-sudo/20-amazeeio-sudo.sh"
      provides: "amazeeio sudoers configuration module"
      min_lines: 25
    - path: "/etc/sudoers.d/amazeeio"
      provides: "Sudoers rule for amazeeio user"
      contains: "amazeeio ALL=(ALL) NOPASSWD: ALL"
      permissions: "440"
  key_links:
    - from: "20-amazeeio-sudo.sh"
      to: "lib/sudo.sh"
      via: "source ${SCRIPT_DIR}/../../lib/sudo.sh"
    - from: "sudoers_create_nopasswd()"
      to: "visudo -c"
      via: "Syntax validation before installation"
---

<objective>
Create amazeeio user sudoers configuration module.

Purpose: Reuse the sudo configuration library to create passwordless sudo access for amazeeio user, following the same security patterns established in 03-01.

Output: modules/30-sudo/20-amazeeio-sudo.sh for amazeeio sudo configuration
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-access-control/03-RESEARCH.md
@lib/common.sh
@lib/sudo.sh
@modules/30-sudo/10-bryan-sudo.sh

## Key Patterns from 03-01

From lib/sudo.sh (created in 03-01):
- sudoers_create_nopasswd(username) - creates validated sudoers rule
- sudoers_validate(file) - validates syntax with visudo -c
- Automatic idempotency (check-before-create)
- Automatic permission setting (440, root:root)
- Mandatory syntax validation before installation

## Module Pattern

Copy the pattern from 10-bryan-sudo.sh:
1. Shebang and strict mode
2. SCRIPT_DIR calculation
3. Source common.sh and sudo.sh
4. Call check_root
5. Call sudoers_create_nopasswd with username

## Security Reminders

- visudo -c validation is automatic in sudoers_create_nopasswd()
- File permissions are set to 440 automatically
- Idempotency prevents unnecessary changes
- User existence is verified before creating rule
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create amazeeio sudoers module (modules/30-sudo/20-amazeeio-sudo.sh)</name>
  <files>modules/30-sudo/20-amazeeio-sudo.sh</files>
  <behavior>
    - Script sources lib/common.sh and lib/sudo.sh
    - Script calls sudoers_create_nopasswd with "amazeeio"
    - Script runs without errors
    - Script is idempotent (can run multiple times)
    - Script creates /etc/sudoers.d/amazeeio with correct content
    - Syntax validation happens automatically via library function
  </behavior>
  <action>
    Create modules/30-sudo/20-amazeeio-sudo.sh following the exact pattern from 10-bryan-sudo.sh:

    1. Shebang and strict mode: #!/bin/bash, set -euo pipefail
    2. Calculate SCRIPT_DIR
    3. Source lib/common.sh (with shellcheck source directive)
    4. Source lib/sudo.sh (with shellcheck source directive)
    5. Call check_root
    6. Log: log_info "Configuring sudo access for amazeeio..."
    7. Call sudoers_create_nopasswd "amazeeio"
    8. Log: log_info "amazeeio sudo configuration complete"

    The module should be identical to 10-bryan-sudo.sh except for the username.
  </action>
  <verify>
    <automated>shellcheck modules/30-sudo/20-amazeeio-sudo.sh</automated>
  </verify>
  <done>20-amazeeio-sudo.sh exists, passes shellcheck, sources libraries correctly</done>
</task>

</tasks>

<verification>
After task completes:
1. File passes shellcheck with no errors
2. 20-amazeeio-sudo.sh follows established patterns
3. Module properly sources common.sh and sudo.sh
4. Module calls sudoers_create_nopasswd with "amazeeio"
</verification>

<success_criteria>
- modules/30-sudo/20-amazeeio-sudo.sh exists and is executable
- File passes shellcheck
- Module properly sources both libraries
- Module calls sudoers_create_nopasswd "amazeeio"
</success_criteria>

<output>
After completion, create `.planning/phases/03-access-control/03-02-SUMMARY.md`
</output>
