---
phase: 08-build-analysis-tools
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/50-docker/57-hadolint.sh
autonomous: true
requirements:
  - LINT-01
  - LINT-02
user_setup: []

must_haves:
  truths:
    - User can run `hadolint Dockerfile` and see lint results
    - hadolint command is available in PATH for all users
    - hadolint can be integrated with editor or CI pipeline (exit codes work correctly)
  artifacts:
    - path: "modules/50-docker/57-hadolint.sh"
      provides: "hadolint installation module"
      min_lines: 80
      exports: ["install_hadolint"]
  key_links:
    - from: "/opt/hadolint/hadolint"
      to: "/usr/local/bin/hadolint"
      via: "symlink"
      pattern: "ln -sf /opt/hadolint/hadolint /usr/local/bin/hadolint"
---

## Objective

Install hadolint Dockerfile linter from GitHub releases using the established binary installation pattern.

**Purpose:** Users need to validate Dockerfiles for best practices, security issues, and optimization opportunities before building images.
**Output:** Executable hadolint binary in /usr/local/bin/ via /opt/hadolint/ installation directory.

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@modules/50-docker/53-lazydocker.sh

## Interfaces

From 53-lazydocker.sh (pattern to follow):
```bash
# Architecture mapping:
# amd64 -> x86_64
# arm64 -> aarch64

# Installation path:
# /opt/lazydocker/ directory
# symlink: /opt/lazydocker/lazydocker -> /usr/local/bin/lazydocker

# GitHub URL pattern:
# https://github.com/jesseduffield/lazydocker/releases/latest/download/lazydocker_latest_Linux_${ARCH}.tar.gz
```

## Installation Method Decision

Per Claude's discretion (08-CONTEXT.md):
- **Method:** Standalone binary from GitHub releases (consistent with lazydocker/dive pattern)
- **Advantages:** Simple, fast, no Docker dependency, works in PATH like other tools
- **Alternative considered:** Docker image (requires docker run, more complex for CI)

## hadolint Specifics

- GitHub: https://github.com/hadolint/hadolint
- Release URL: https://github.com/hadolint/hadolint/releases/latest/download/hadolint-Linux-${ARCH}
- Note: hadolint releases are single binaries (NOT tar.gz), direct download
- Binary name: hadolint-Linux-x86_64 or hadolint-Linux-arm64

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create hadolint installation module</name>
  <files>modules/50-docker/57-hadolint.sh</files>
  <action>
    Create modules/50-docker/57-hadolint.sh following the lazydocker pattern.

    Key implementation details:
    1. Source common.sh: source "$(dirname "$0")/../../lib/common.sh"
    2. Idempotency: Check if /opt/hadolint/hadolint exists AND command -v hadolint works
    3. Architecture detection: dpkg --print-architecture → map amd64→x86_64, arm64→aarch64
    4. Download URL: https://github.com/hadolint/hadolint/releases/latest/download/hadolint-Linux-${ARCH}
       - Note: hadolint uses direct binary download, NOT tar.gz
    5. Create /opt/hadolint/ directory (mkdir -p)
    6. Download directly to /opt/hadolint/hadolint (not to temp dir first)
    7. Make binary executable: chmod +x /opt/hadolint/hadolint
    8. Create symlink: ln -sf /opt/hadolint/hadolint /usr/local/bin/hadolint
    9. Verify with hadolint --version
    10. Log progress with log_info, use die for fatal errors

    Important differences from lazydocker:
    - hadolint is a single binary (not tar.gz)
    - Download directly to final location, no extraction needed
    - URL format: hadolint-Linux-${ARCH} (not .tar.gz suffix)

    Pattern elements to keep:
    - Architecture detection
    - /opt/ + symlink structure
    - Idempotency checks
    - Error handling
    - Logging

    Make script executable: chmod +x
  </action>
  <verify>
    <automated>bash -n modules/50-docker/57-hadolint.sh && echo "Syntax OK"</automated>
  </verify>
  <done>
    - Script exists at modules/50-docker/57-hadolint.sh
    - Script is executable (chmod +x applied)
    - Syntax validation passes (bash -n)
    - Script follows lazydocker pattern structure
    - Script handles hadolint's single-binary distribution (not tar.gz)
    - Includes proper shebang, error handling, and logging
  </done>
</task>

</tasks>

<verification>

## Code Review Checklist

- [ ] Script follows bash patterns from lazydocker module
- [ ] Uses `set -euo pipefail` for strict mode
- [ ] Sources common.sh with correct relative path
- [ ] Architecture detection handles amd64 (x86_64) and arm64 (aarch64)
- [ ] Uses correct GitHub 'latest' URL for hadolint releases
- [ ] Handles single-binary distribution (not tar.gz like lazydocker)
- [ ] Idempotency check before download/install
- [ ] Creates /opt/hadolint/ directory
- [ ] Downloads binary directly (not tar.gz extraction)
- [ ] Makes binary executable with chmod +x
- [ ] Creates symlink in /usr/local/bin/
- [ ] Includes error handling with die() for critical failures
- [ ] Script is executable (chmod +x)

</verification>

<success_criteria>

1. modules/50-docker/57-hadolint.sh exists and is executable
2. Script follows established bash patterns from lazydocker
3. Script implements hadolint installation via GitHub releases
4. Script handles hadolint's single-binary distribution correctly
5. Script uses /opt/hadolint/ + symlink pattern
6. Script supports amd64 and arm64 architectures
7. Script includes idempotency checks

</success_criteria>

<output>
After completion, create `.planning/phases/08-build-analysis-tools/08-03-SUMMARY.md`
</output>
