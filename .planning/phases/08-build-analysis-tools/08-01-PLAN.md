---
phase: 08-build-analysis-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/50-docker/55-dive.sh
autonomous: true
requirements:
  - DIVE-01
  - DIVE-02
user_setup: []

must_haves:
  truths:
    - User can run `dive <image>` and explore image layers interactively
    - dive command is available in PATH for all users
  artifacts:
    - path: "modules/50-docker/55-dive.sh"
      provides: "dive installation module"
      min_lines: 80
      exports: ["install_dive"]
  key_links:
    - from: "/opt/dive/dive"
      to: "/usr/local/bin/dive"
      via: "symlink"
      pattern: "ln -sf /opt/dive/dive /usr/local/bin/dive"
---

## Objective

Install dive Docker image analyzer from GitHub releases using the established binary installation pattern.

**Purpose:** Users need to analyze Docker image layers to optimize image sizes and understand layer composition.
**Output:** Executable dive binary in /usr/local/bin/ via /opt/dive/ installation directory.

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@modules/50-docker/53-lazydocker.sh

## Interfaces

From 53-lazydocker.sh (pattern to follow):
```bash
# Architecture mapping:
# amd64 -> x86_64
# arm64 -> aarch64

# Installation path:
# /opt/lazydocker/ directory
# symlink: /opt/lazydocker/lazydocker -> /usr/local/bin/lazydocker

# GitHub URL pattern:
# https://github.com/jesseduffield/lazydocker/releases/latest/download/lazydocker_latest_Linux_${ARCH}.tar.gz
```

## Locked Decision

Per user decision in 08-CONTEXT.md:
- **Installation method:** GitHub binary release (like lazydocker)
- **Version strategy:** Latest release using 'latest' URL
- **Install location:** /opt/dive/ directory
- **Symlink:** /usr/local/bin/dive
- **Architecture:** Support amd64 (x86_64) and arm64 (aarch64)

## dive Specifics

- GitHub: https://github.com/wagoodman/dive
- Release URL: https://github.com/wagoodman/dive/releases/latest/download/dive_Linux_${ARCH}.tar.gz
- Binary name: dive (extracted from tar.gz)

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create dive installation module</name>
  <files>modules/50-docker/55-dive.sh</files>
  <action>
    Create modules/50-docker/55-dive.sh following the lazydocker pattern (53-lazydocker.sh).

    Key implementation details:
    1. Source common.sh at top using: source "$(dirname "$0")/../../lib/common.sh"
    2. Idempotency: Check if /opt/dive/dive exists AND command -v dive works before installing
    3. Architecture detection: dpkg --print-architecture → map amd64→x86_64, arm64→aarch64
    4. Download URL: https://github.com/wagoodman/dive/releases/latest/download/dive_Linux_${ARCH}.tar.gz
    5. Extract to /opt/dive/ directory (mkdir -p first)
    6. Create symlink: ln -sf /opt/dive/dive /usr/local/bin/dive
    7. Verify with dive --version
    8. Use log_info for progress messages, die for fatal errors
    9. Include proper error handling and cleanup of temp files

    Pattern reference from lazydocker:
    - Use mktemp -d for temp directory
    - Use curl -fsSL for download
    - Use tar -xzf for extraction
    - Clean up temp dir with rm -rf

    Make script executable: chmod +x
  </action>
  <verify>
    <automated>bash -n modules/50-docker/55-dive.sh && echo "Syntax OK"</automated>
  </verify>
  <done>
    - Script exists at modules/50-docker/55-dive.sh
    - Script is executable (chmod +x applied)
    - Syntax validation passes (bash -n)
    - Script follows lazydocker pattern structure
    - Includes proper shebang, error handling, and logging
  </done>
</task>

</tasks>

<verification>

## Code Review Checklist

- [ ] Script follows bash patterns from lazydocker module
- [ ] Uses `set -euo pipefail` for strict mode
- [ ] Sources common.sh with correct relative path
- [ ] Architecture detection handles amd64 and arm64
- [ ] Uses GitHub 'latest' URL for dive releases
- [ ] Idempotency check before download/install
- [ ] Creates /opt/dive/ directory
- [ ] Creates symlink in /usr/local/bin/
- [ ] Includes error handling with die() for critical failures
- [ ] Script is executable (chmod +x)

</verification>

<success_criteria>

1. modules/50-docker/55-dive.sh exists and is executable
2. Script follows established bash patterns from lazydocker
3. Script implements dive installation via GitHub releases
4. Script uses /opt/dive/ + symlink pattern
5. Script supports amd64 and arm64 architectures
6. Script includes idempotency checks

</success_criteria>

<output>
After completion, create `.planning/phases/08-build-analysis-tools/08-01-SUMMARY.md`
</output>
